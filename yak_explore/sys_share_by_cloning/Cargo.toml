[package]
name = "sys_share_by_cloning"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time", "sync"]}
rand = "0.8"
async-trait = "0.1"